# Work Plan

Tracked in ClickUp: list **Helm - Claude Code Shell** (`901114291892`).
Priority-ordered; work top-down. Full descriptions and acceptance criteria live on the tasks.

| ClickUp | Task | Gate |
|---|---|---|
| ~~[868knyagd](https://app.clickup.com/t/868knyagd)~~ | Spike A: Overlay composition via `--plugin-dir` | **GO** - the premise holds |
| ~~[868knyagj](https://app.clickup.com/t/868knyagj)~~ | Spike B: Portable packaging with native modules | **GO** - build config is the repo seed ([SPIKE-B.md](SPIKE-B.md)) |
| ~~[868knyagp](https://app.clickup.com/t/868knyagp)~~ | Spike C: Claude TUI fidelity inside xterm.js | **GO, embedded-first**, no external fallback ([SPIKE-C.md](SPIKE-C.md)) |
| ~~[868knyagz](https://app.clickup.com/t/868knyagz)~~ | M1: Foundation - shell, core, SQLite, discovery | **DONE** - monorepo, store, discovery, window shell |
| ~~[868knyah0](https://app.clickup.com/t/868knyah0)~~ | M2: Embedded terminal - claude in tabs | **DONE** - sessions, tabs, teardown; `pnpm m2-check` is the regression test. Spike C's human soak deferred into real use (`notes/task-helm-tui-soak.md`) |
| ~~[868knyah6](https://app.clickup.com/t/868knyah6)~~ | M3: Profiles + overlay composition | **GO - the premise holds.** Profiles in SQLite with YAML round-trip, junction shims, argv builder; `pnpm m3-check` is the regression test. `--add-dir` does not carry an overlay's CLAUDE.md; `--append-system-prompt-file` does |
| ~~[868knyah9](https://app.clickup.com/t/868knyah9)~~ | M4: Session launcher - resume across projects | **DONE** - 799 sessions / 36 projects indexed from `history.jsonl`, searchable in ~3 ms, resumable ones marked honestly (106 of 799); `pnpm m4-check` is the regression test. `--resume` resolves the id against the cwd, so a session can only be reopened from its own folder |
| ~~[868knyahd](https://app.clickup.com/t/868knyahd)~~ | M5: Config console + effective view | **DONE** - browse and edit any scope's `.claude`, effective view proved against a live session, MCP through `claude mcp add-json`, every write snapshotted; `pnpm m5-check` is the regression test. Settings merge per leaf, not per top-level key |
| ~~[868knyahg](https://app.clickup.com/t/868knyahg)~~ | M6: Content viewer - markdown/HTML/wikilinks | **DONE** - markdown, HTML artifacts in a sandboxed frame, wikilinks, split editor with snapshotted saves; `pnpm m6-check` is the regression test |
| [868knyahn](https://app.clickup.com/t/868knyahn) | M7: Portable packaging + first-run | **Built, one criterion unverifiable here.** Setup pane (locate the CLI, detect a sign-in without touching one, choose roots, scaffold a harness), `repos:` key so converting a folder does not hide its repositories, version guard that warns and gates nothing; portable exe and NSIS installer both install-tested - the gap Spike B left - with an unelevated per-user install, `%APPDATA%` data and a clean uninstall. No auto-updater, and [PACKAGING.md](PACKAGING.md) says why. `pnpm m7-check` is the regression test and drives a whole second app against an empty profile. "A second machine goes download → session in 5 minutes using only the README" needs a second machine |
| ~~[868kp18rk](https://app.clickup.com/t/868kp18rk)~~ | Status bar: session/weekly usage, % or estimated dollars | **DONE** - percentages read from `~/.claude.json` → `cachedUsageUtilization` and matched against a live session's own `/usage` (29% / 45%, three ways); dollars estimated from an incremental index over 214 MB of transcripts and reconciled to ten significant figures with a hand-written parse; `pnpm usage-check` is the regression test. A reading Helm cannot stand behind shows nothing at all |
| [868kpn0ju](https://app.clickup.com/t/868kpn0ju) | M8: Settings pane - app-level configuration surface | **DONE** - a `{kind:'settings'}` pane behind the title bar's gear, with the CLI override finally reachable after first run, `roots:remove` finally given a caller, and theme and usage display settable beside their quick accessors, which stay. `app_settings` needed no migration; it needed validation, which it had none of - `{theme:'purple'}` used to persist and reach `nativeTheme.themeSource`. Writes are now strict per key and a patch applies as one edit, while reads stay tolerant. `pnpm settings-check` is the regression test: 9 checks, two phases, every UI assertion beside the driver's own read-only connection to `helm.db` |
| [868kpn9zu](https://app.clickup.com/t/868kpn9zu) | M9: Terminal customization - font, size, shell choice | **DONE** - a Terminal group in the settings pane (font, size, cursor, blink, scrollback, default shell) applying live to every open terminal in both registries; the chosen family is prepended to the built-in stack rather than replacing it, so a font with no box-drawing degrades a glyph at a time. `estimateGrid` reads the same settings and now measures the way xterm does - a DOM span, the WebGL renderer's device-pixel flooring, and FitAddon's 14px ruler reserve - which took it from eight columns out at 20px to exact. Shell choice is a default plus a per-pane picker, read per open so no restart is needed, with a per-shell args table replacing a filename substring test that would have handed `bash` a `-NoLogo`. Colour is deliberately not settable. `pnpm settings-check` is the regression test: 12 checks, of which S-10 to S-12 are M9's |
| [868kpmya4](https://app.clickup.com/t/868kpmya4) | M10: GitHub PRs - fetch, list pane, sidebar row | **DONE** - the first surface whose subject is not on this machine. `core/github/` parses every remote form git writes and strips an embedded token out of one before the slug is stored; two cache tables let the pane paint from SQLite on the first frame; the sweep is one `gh pr list --repo <slug>` per distinct remote on a five-minute timer that survives a pass which throws. Degradation is stale-with-age rather than degrade-to-nothing, which is the opposite of the usage figures and deliberately so - a pull request that was open two hours ago is a true fact about two hours ago, so the cached rows stay, the reason goes on the repository that failed, and the age caption is mandatory rather than decorative. Helm holds no GitHub credential: `gh` owns the token, and a sign-in is read from the exit code of `gh auth status` and nothing else |
| [868kpmyjc](https://app.clickup.com/t/868kpmyjc) | M11: GitHub PRs - detail tab | **DONE** - Conversation, Commits and Files behind a segmented control, laid out as GitHub's own page is because that is the shape anybody opening it already knows. Markdown is rendered in **main** through the content viewer's pipeline, so the window receives HTML it never evaluates and shiki's grammars stay out of the browser bundle. `statusCheckRollup` is a GraphQL union whose members agree on nothing, so it reduces to three numbers and paints nothing at all when it cannot be read - null and `{total: 0}` are different facts and only one is safe to show as a green tick. Not a diff viewer: the Files view lists paths and counts and links out, and says so. Inline diff-thread comments are invisible to `gh --json` at all, which the conversation admits at the bottom. Proved 9/9 against a real public pull request in an isolated portable profile |
| [868kpmyq3](https://app.clickup.com/t/868kpmyq3) | M12: GitHub PRs - review launch, `pnpm pr-check`, doc amendments | **DONE** - a "Review with Claude" button that starts an ordinary hosted session with the review as its opening prompt, riding the trailing-positional path `buildLaunchArgs` already had. The prompt is composed in **main**: `pr:review` carries `{repoPath, number, cols, rows}` and nothing else, the same shape `profile:launch` takes, because argv assembled in a renderer is argv that can drift from what was saved - proved by planting a prompt on the wire and watching it reach no argv. Two settings in the pane's GitHub group: a template whose unknown placeholders survive as written, so a typo shows up in the pane's disclosure sentence rather than as a word missing from the argv, and a checkout mode refused outright on a dirty tree because Helm does not move somebody's uncommitted work. Helm reads none of the review back. The network posture was amended in the open across README, PACKAGING.md, `shared/ipc.ts`, SPEC 5 and CLAUDE.md: the update check is the only *direct* request Helm makes. `pnpm pr-check` is the regression test - 13 checks in five phases, four against a `gh` this repository wrote and one against the real one, with the fixture mutated underneath the comparator before its clean pass is believed |
| [868knyahu](https://app.clickup.com/t/868knyahu) | Backlog: Transcript archive (v1.1) | Deferred |
